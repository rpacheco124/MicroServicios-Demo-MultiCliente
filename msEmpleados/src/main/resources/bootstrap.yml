spring:
  cloud:
    config:
      uri: http://localhost:8888/
  application:
    name: empleados
server:
  port: 0
security:
  oauth2:
    resource:
      id: empleados
feign: 
  hystrix: 
    enabled: true