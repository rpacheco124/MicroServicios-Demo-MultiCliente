spring:
  cloud:
    config:
      uri: http://localhost:8888/
  application:
    name: personas
server:
  port: 0
security:
  oauth2:
    resource:
      id: personas
feign: 
  hystrix: 
    enabled: true